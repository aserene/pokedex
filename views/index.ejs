<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <%- include("./partials/header.ejs") %>
    <div class="container">
        <div class="div-transform">
            <% for(poke of pokemon) { %>
                <div class="card">
                    <a href="/pokedex/<%=poke.name%>"><h2><%=poke.name%></h2></a>
                </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>